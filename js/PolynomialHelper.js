function PolynomialHelper() {

    this.getPolynomialForFiniteField = function(iMaxPolynomialDegree, aAcceptableCoefficients = []) {
        var aResultPolynomials = [];
        for (let i = 0; i < iMaxPolynomialDegree; i++) {
            for (let value of aAcceptableCoefficients) {

            }
        }
    }

    return this;

}